const mongoose = require("mongoose")

const PaymontCetogarySchema = new mongoose.Schema( {

    

})

const Paymontcetogary = mongoose.model("PaymontCetogarySchema" , PaymontCetogarySchema);


module.exports = Paymontcetogary;